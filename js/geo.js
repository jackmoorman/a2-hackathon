// geo.js — coordinate + projection math, isolated so it can be reasoned about
// (and later unit-tested) without a phone. Fleshed out in a later milestone.
// For milestone 1 this is a placeholder namespace.

window.Geo = {
  // meters-per-degree helpers will live here (lat/lng <-> local meters),
  // bearing math, and the world->screen pinhole projection.
};
